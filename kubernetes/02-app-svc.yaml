apiVersion: v1
kind: Service
metadata:
  name: #{APP_NAME}-service
  namespace: #{APP_NAME}
  labels:
    app: #{APP_NAME}
    env: #{DEPLOYMENT}
spec:
  type: LoadBalancer
  selector:
    release: v#{VERSION}
  ports:
    - port: 80
      targetPort: 5000